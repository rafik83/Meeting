apiVersion: v1
kind: Secret
metadata:
  name: {{ template "vimeet.fullname" . }}-parameters
  labels:
    app: "{{ template "vimeet.name" . }}"
    chart: "{{ template "vimeet.chart" . }}"
    release: {{ .Release.Name | quote }}
    heritage: {{ .Release.Service | quote }}
  annotations:
    strategy.spinnaker.io/versioned: 'false'
type: Opaque
stringData:
  parameters.yml:{{ toYaml .Values.parameters.parameters_yml | indent 2 }}
