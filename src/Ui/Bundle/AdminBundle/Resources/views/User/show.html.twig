{% extends 'AdminBundle::base.html.twig' %}

{% import 'AdminBundle::macro.html.twig' as macro %}

{% block content %}
    <div>
        <h1 class="page-header">{{ ('gender.' ~ user.account.gender)|trans }} {{ user.account.completeName }} - {{ user.account.position }}</h1>
        <div class="col-md-4">
            {% if user.account.avatar is not empty %}
                <img src="{{ user.account.avatar|imagine_filter('user_avatar') }}" class="img-responsive img-thumbnail">
            {% endif %}
            <div>
                <div class="coordinates">
                    <h4>{{ 'admin.user.contact-information'|trans }} :</h4>
                    <ul>
                        <li>
                            <i class="glyphicon glyphicon-home"></i>
                            {{ user.account.address }}, {{ user.account.zipCode }}, {{ user.account.city }}, {{ user.account.country|country }}
                        </li>
                        <li><i class="glyphicon glyphicon-envelope"></i> {{ user.email }}</li>
                        <li><i class="glyphicon glyphicon-earphone"></i> {{ user.account.phone }}</li>
                        <li><i class="glyphicon glyphicon-phone"></i> {{ user.account.mobile }}</li>
                        {% if user.account.website is not empty %}
                            <li>
                                <a href="{{ user.account.website }}" target="_blank">
                                    {{ user.account.website }}
                                </a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
            <div class="coordinates">
                <h4>{{ 'admin.user.company-information'|trans }}:</h4>
                <ul>
                    <li>{{ user.account.company }}</li>
                    <li>{{ user.account.companyAddress }}</li>
                    <li>{{ user.account.companyZipCode }} - {{ user.account.companyCity }}, {{ user.account.companyCountry|country }}</li>
                    <li>
                        <a href="{{ user.account.companyWebsite }}" target="_blank">
                            {{ user.account.companyWebsite }}
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </div>

    <div class="form-group submit-section">
        <div class="col-sm-3">
            <a href="{{ path('admin_users', {'event': event.id}) }}" class="btn btn-link">
                <span class="glyphicon glyphicon-arrow-left"></span>
                {{ 'admin.goback.link'|trans }}
            </a>
        </div>
    </div>
{% endblock %}
