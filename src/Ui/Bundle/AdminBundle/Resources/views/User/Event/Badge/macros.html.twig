{% macro write_badge(userBadgeByEventView, side) %}
<div class="quarterSection-container {% if side == 'right' %}quarterSection-container--upRight{% endif %}">
    <div class="quarterSection badge">
        {% if userBadgeByEventView.header %}
            <div style="background: {{ userBadgeByEventView.headerLeftColor }};
                background: -moz-linear-gradient(left, {{ userBadgeByEventView.headerLeftColor }} 0%, {{ userBadgeByEventView.headerRightColor }} 100%);
                background: -webkit-linear-gradient(left, {{ userBadgeByEventView.headerLeftColor }} 0%, {{ userBadgeByEventView.headerRightColor }} 100%);
                background: linear-gradient(to right, {{ userBadgeByEventView.headerLeftColor }} 0%, {{ userBadgeByEventView.headerRightColor }} 100%);
                filter: progid:DXImageTransform.Microsoft.gradient(startColorstr={{ userBadgeByEventView.headerLeftColor }}, endColorstr={{ userBadgeByEventView.headerRightColor }},GradientType=1);"
            >
                <img src="{{ userBadgeByEventView.header }}" class="badge-image badge-image--logo print-bg" />
            </div>
        {% endif %}

        <div class="badge-content">
            <div class="company-participant {% if not userBadgeByEventView.qrCodeIdentifier %}company-zone-without-qrcode{% endif %}">
                <div class="text">{{ userBadgeByEventView.sheetTitle }}</div>
            </div>

            {% if userBadgeByEventView.qrCodeIdentifier %}
                <div class="qrcode">
                    <div class="qrcode-border">
                        <img src="{{ userBadgeByEventView.qrCodeImage }}"/><br>
                        ID {{ userBadgeByEventView.qrCodeIdentifier }}<br>
                    </div>
                </div>
            {% endif %}

            <div class="info-participant {% if not userBadgeByEventView.qrCodeIdentifier %}participant-zone-without-qrcode{% endif %}">
                <div class="name-participant {% if not userBadgeByEventView.qrCodeIdentifier %}text-participant-without-qrcode{% endif %}">
                    <div class="text">
                        {{ userBadgeByEventView.firstName }}
                        <span class="last-name-participant">{{ userBadgeByEventView.lastName }}</span>
                    </div>
                </div>

                <div class="position-participant {% if not userBadgeByEventView.qrCodeIdentifier %}text-participant-without-qrcode{% endif %}">
                    <div class="text">
                        {{ userBadgeByEventView.position }}
                        {% if userBadgeByEventView.country %}
                            <br/>{{ userBadgeByEventView.country|upper }}
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>

        <div class="type-participant" style="color: {{ userBadgeByEventView.footerTextColor }}; background-color: {{ userBadgeByEventView.footerColor }}">
            {% if userBadgeByEventView.participationTypeOrCategoryLabel %}
                {{ userBadgeByEventView.participationTypeOrCategoryLabel }}<br>
            {% endif %}
        </div>
    </div>
</div>
{% endmacro %}

{% macro write_image(imagePath, side, fullHeight = false) %}
    {% if side == 'left' %}
        {% set sectionContainerClasses = 'quarterSection-container quarterSection-container--downLeft' %}
        {% set sectionClass = 'quarterSection' %}
    {% elseif fullHeight == false %}
        {% set sectionContainerClasses = 'quarterSection-container quarterSection-container--downRight' %}
        {% set sectionClass = 'quarterSection' %}
    {% else %}
        {% set sectionContainerClasses = 'halfXSection-container halfXSection-container--right' %}
        {% set sectionClass = 'halfXSection' %}
    {% endif %}
    <div class="{{ sectionContainerClasses }}">
        <div class="{{ sectionClass }}">
            <img src="{{ imagePath }}" class="badge-image print-bg" />
        </div>
    </div>
{% endmacro %}
