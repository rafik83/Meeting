{% extends 'AdminBundle::base.html.twig' %}

{% import 'AdminBundle::macro.html.twig' as macro %}

{% block title 'admin.type.registrationPath.title'|trans %}

{% block content %}
    <h1 class="page-header">{{ 'admin.type.registrationPath.title'|trans }}</h1>

    {% if not eventRegistrationPathView.isPathCompleted %}
        <div class="alert alert-danger">{{ 'admin.type.registrationPath.pathNotCompleted'|trans }}</div>
    {% endif %}

    {% if eventRegistrationPathView.questionView %}
        {{ include('@Admin/Type/RegistrationPath/questionView.html.twig', { questionView: eventRegistrationPathView.questionView }) }}
    {% else %}
        <a href="{{ path('admin_type_registration_path_add_question', {event: event.id}) }}" class="btn btn-info">{{ 'admin.type.registrationPath.addQuestion'|trans }}</a>
    {% endif %}
{% endblock %}
