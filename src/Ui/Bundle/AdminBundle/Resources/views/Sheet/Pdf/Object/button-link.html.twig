{% if item.data['url']|defaul('#') != '#' %}
<div id="object-{{ uid }}" class="object {{ 'style--' ~ item.style }}">
    <p>
        <i class="icon-Desktop"></i> {{ item.data['url']|default('#') }}
    </p>
</div>
{% endif %}
