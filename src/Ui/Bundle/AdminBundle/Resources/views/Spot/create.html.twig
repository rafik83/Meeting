{% extends 'AdminBundle:Common:form_layout.html.twig' %}

{% block form_title 'admin.spot.createSpot.title'|trans %}

{% block form_content %}
    {{ form_row(form.reference, {widget_columns: 2}) }}
    {{ form_row(form.size, {widget_columns: 2}) }}
    {{ form_row(form.meetingCapacity, {widget_columns: 2}) }}
    {{ form_row(form.seatCapacity, {widget_columns: 2}) }}
    {{ form_row(form.priority) }}
    {{ form_row(form.active) }}
    {{ form_row(form.visio) }}
{% endblock form_content %}

{% block goback_link path('admin_spot_list', {'event': event.id}) %}
