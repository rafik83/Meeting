{% extends 'AdminBundle::base.html.twig' %}

{% block content %}

    <h1><a href="{{ path('admin_event_read', {event: event.id}) }}">{{ event.title }}</a></h1>

    <h2>{{ 'admin.spot.createSpot.title'|trans }}</h2>

    {{ form(form) }}

{% endblock %}

