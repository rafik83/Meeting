{% extends 'AdminBundle::base.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-md-3">
            <a href="{{ path('admin_template_sheet_list') }}" class="btn btn-primary">Template de Participation</a>
        </div>
        <div class="col-md-3">
            <a href="{{ path('admin_template_registration_list') }}" class="btn btn-primary">Template d'Inscription</a>
        </div>
    </div>
{% endblock %}
