{% extends 'AdminBundle::base.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-md-3">
            <a href="{{ path('admin_template_sheet_list') }}" class="btn btn-primary">{{ 'admin.template.sheet.link'|trans }}</a>
        </div>
        <div class="col-md-3">
            <a href="{{ path('admin_template_registration_list') }}" class="btn btn-primary">{{ 'admin.template.registration.link'|trans }}</a>
        </div>
    </div>
{% endblock %}
