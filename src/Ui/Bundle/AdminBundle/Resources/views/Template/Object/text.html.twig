{% trans_default_domain 'templates' %}

{% set config = {
    content: { (locale): 'template.object.text'|trans },
    type: 'titre',
    style: ''
}|merge(config|default({})) %}

<div class="object" data-object="text" data-uid="{{ uid|default('__UID__') }}" data-config="{{ config|json_encode }}">
    <ul class="block-buttons list-inline">
        <li class="object-title">{{ 'template.object.text'|trans }}</li>
        <li>
            <span class="btn btn-link move-button">
                <i class="glyphicon glyphicon-move"></i>
            </span>
        </li>
        <li>
            <button class="btn btn-link delete-button">
                <i class="glyphicon glyphicon-remove"></i>
            </button>
        </li>
        <li>
            <button class="btn btn-link configure-button">
                <i class="glyphicon glyphicon-cog"></i>
            </button>
            <div class="modal fade configure-modal" tabindex="-1" role="dialog">
                <form action="#" method="GET">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-header">
                                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                                <h4 class="modal-title">{{ 'template.object.text'|trans }}</h4>
                            </div>
                            <div class="modal-body">
                                <div class="form-group">
                                    <label class="form-label">{{ 'template.style.title'|trans }}</label>
                                    <select class="form-control" name="style" title="Style">
                                        <option value="style-1">Style 1</option>
                                        <option value="style-2">Style 2</option>
                                    </select>
                                </div>
                                <hr />
                                <div class="form-group">
                                    <label class="form-label">{{ 'template.content.title'|trans }}</label>
                                    <textarea class="form-control" name="content"></textarea>
                                    {% if fallback|default(false) and locale != fallback %}
                                        <div class="help-block">{{ fallback|locale }} : {{ config.content[fallback] }}</div>
                                    {% endif %}
                                    <a href="https://commonmark.org/help/" target="_blank">{{ 'template.content.text.link'|trans }}</a>
                                </div>
                                <div class="form-group">
                                    <label class="form-label">{{ 'template.type.title'|trans }}</label>
                                    <select class="form-control" name="type">
                                        {% for label, value in { 'Titre': 'title', 'Texte': 'text' } %}
                                            <option value="{{ value }}">{{ label }}</option>
                                        {% endfor %}
                                    </select>
                                </div>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-default" data-dismiss="modal">{{ 'template.action.close'|trans }}</button>
                                <button type="button" class="btn btn-primary save-configuration">{{ 'template.action.save'|trans }}</button>
                            </div>
                        </div>
                    </div>
                </form>
            </div>
        </li>
    </ul>
    <div data-bind="content">{{ config.content[locale] }}</div>
</div>
