<div class="form-group">
    <label class="form-label">{{ 'template.object.visibility.label'|trans({}, 'templates') }}</label>
    <div class="radio">
        <label>
            <input type="radio" name="visibility" value="editable" checked="checked"/> {{ 'template.object.visibility.editable'|trans({}, 'templates') }}
        </label>
    </div>
    <div class="radio">
        <label>
            <input type="radio" name="visibility" value="read_only"/> {{ 'template.object.visibility.read_only'|trans({}, 'templates') }}
        </label>
    </div>
    <div class="radio">
        <label>
            <input type="radio" name="visibility" value="hidden"/> {{ 'template.object.visibility.hidden'|trans({}, 'templates') }}
        </label>
    </div>
</div>
