{% extends 'AdminBundle::base.html.twig' %}

{% block title 'admin.event.dashboard.title'|trans %}

{% block content %}
    <h1>{{ 'admin.event.dashboard.title'|trans }}</h1>

    {{ include('AdminBundle:Dashboard:orders.html.twig', {
        dashboard: dashboard.transactionView,
        event: event
    }, false) }}

    {{ include('AdminBundle:Dashboard:meetings.html.twig', {
        dashboard: dashboard.dashboardMeetingView,
        event: event
    }, false) }}

    {{ include('AdminBundle:Dashboard:sheets.html.twig', {
        dashboard: dashboard,
        event: event
    }, false) }}

{% endblock %}
