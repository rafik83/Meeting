{% extends 'MailBundle:Mail:base.html.twig' %}

{% import "MailBundle:Mail:macros.html.twig" as macros %}

{% block body %}
    {{ 'mail.sheet.validation.draft.body'|trans({
        '%type%': mail.type
    }, 'mail', mail.locale)|html|nl2br }}

    <br>
    <br>

    {{ macros.button(
        'mail.sheet.show.link'|trans({}, 'mail', mail.locale),
        'https://' ~ mail.event.domain ~ path('event_sheet')
    ) }}
{% endblock %}
