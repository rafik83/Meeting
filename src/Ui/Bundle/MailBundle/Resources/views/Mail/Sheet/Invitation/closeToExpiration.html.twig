{% block subject %}
    {{ 'mail.sheet.invitation_close_to_expiration.subject'|trans }}
{% endblock %}

{% block body %}
    {{ 'mail.sheet.invitation_close_to_expiration.body'|trans({
        '%firstname%': mail.user.firstname|humanize,
        '%lastname%': mail.user.lastname|upper,
        '%event%': mail.event.title
    }, 'mail', mail.locale)|html|nl2br }}
{% endblock %}

{% block end %}
    {{ 'mail.sheet.invitation_close_to_expiration.end'|trans({
        '%event%': mail.event.title,
        '%eventPhone%': '',
        '%eventMail%': ''
    }, 'mail', mail.locale) }}
{% endblock %}
