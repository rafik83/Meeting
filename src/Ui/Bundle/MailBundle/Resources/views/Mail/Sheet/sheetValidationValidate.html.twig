{% import "MailBundle:Mail:macros.html.twig" as macros %}

{% block body %}
    {{ 'mail.sheet.validation.validate.body'|trans({}, 'mail', mail.locale)|html|nl2br }}

    <br>
    <br>

    {{ macros.button(
        'mail.sheet.show.link'|trans({}, 'mail', mail.locale),
        '%sheetLink%'
    ) }}
{% endblock %}
