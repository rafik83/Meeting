{% import "MailBundle:Mail:macros.html.twig" as macros %}

{% for invoiceUrlView in invoiceUrlViews  %}
    {{ macros.button(
        'mail.sheet.invoiced.link'|trans({'%invoiceNumber%': invoiceUrlView.number}, 'mail', mail.locale),
        invoiceUrlView.url
    ) }}
{% endfor %}
