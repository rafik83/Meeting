{% import "MailBundle:Mail:macros.html.twig" as macros %}
<html>
<body>
{{ 'mail.product.export.intro'|trans({'%eventTitle%': mail.event.title}, 'mail', mail.locale)|html|nl2br }}
<br>
{{ macros.button(
    'mail.product.export.link.click'|trans({}, 'mail', mail.locale),
    url('admin_product_export_file', {
        'event': mail.event.id,
        'hash': mail.fileHash,
        'file': mail.fileId,
        '_locale': mail.locale,
    })
) }}
</body>
</html>
