{% extends 'MailBundle:Mail:base.html.twig' %}

{% import "MailBundle:Mail:macros.html.twig" as macros %}

{% block body %}
    {{ 'admin.mail.export_rooming_list.body'|trans({}, 'mail', mail.locale)|html|nl2br }}

    <br>
    <br>
    {{ macros.button(
        'admin.mail.export_rooming_list.file_link'|trans({}, 'mail', mail.locale),
        url('admin_event_rooming_list_download_export', {'event': mail.event.id, 'file': mail.file.id, 'hash': mail.file.hash })
    ) }}
    <br>

    {{ 'admin.mail.export_rooming_list.end'|trans({}, 'mail', mail.locale)|html|nl2br }}
{% endblock %}
