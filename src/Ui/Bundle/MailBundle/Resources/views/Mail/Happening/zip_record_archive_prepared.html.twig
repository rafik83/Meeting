{% extends 'MailBundle:Mail:base.html.twig' %}

{% import "MailBundle:Mail:macros.html.twig" as macros %}

{% block body %}
    <p>{{ 'mail.happening.webinar.zip_record_archive_prepared.intro'|trans({}, 'mail', mail.locale) }}</p>
    <br>
    {{ macros.button(
        'mail.happening.webinar.zip_record_archive_prepared.link'|trans({},'mail', mail.locale),
        mail.webinarRecordZipFileUrl
    ) }}
    <br>
    <p>{{ 'mail.happening.webinar.zip_record_archive_prepared.outro'|trans({}, 'mail', mail.locale) }}</p>
{% endblock %}
