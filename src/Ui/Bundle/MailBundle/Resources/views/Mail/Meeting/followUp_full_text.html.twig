{# This template is used to feed admin text editor. That's why there's no value for placeholders #}
<p>{{ 'mail.meeting.followUp.body'|trans({}, 'mail', locale)|html|nl2br }}</p>

%metParticipants%

<p>{{ 'mail.meeting.followUp.end'|trans({}, 'mail', locale)|html|nl2br }}</p>
