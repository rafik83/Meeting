{% if form.vars.value|length == 0 and form.vars.choices|length <= 1 %}
    {{ form_widget(form, { attr: { class: 'hide' } }) }}
{% else %}
<div class="filter">
    <h3>{{ form.vars.label }}</h3>
        {{ form_widget(form) }}
</div>
{% endif %}
