{% set resultsCount = paginatedResult.results|length %}

{% extends 'EventBundle::base.html.twig' %}

{% block title 'navigation.category.catalog'|trans %}

{% block body_attributes 'class="grey"' %}

{% block header_mobile %}
    <div class="catalog-mobile-menu">
        {% if searchForm.type is defined or searchForm.availableSlot is defined %}
            <div class="catalog-mobile-menu-summary catalog-mobile-menu-container">
                {% if searchForm.availableSlot is defined %}
                    <a href="#" class="catalog-mobile-menu-button btn btn-primary btn-block">
                        <span>...</span>
                        <span class="button-count">
                        <i class="glyphicon glyphicon-chevron-down"></i>
                    </span>
                    </a>
                {% endif %}
                {% if searchForm.type is defined %}
                    <a href="#" class="catalog-mobile-menu-button btn btn-primary btn-block">
                        <span>...</span>
                        <span class="button-count">
                    (...)
                    <i class="glyphicon glyphicon-chevron-down"></i>
                </span>
                    </a>
                {% endif %}
                {% if searchForm.content is defined %}
                    <a href="#" class="catalog-mobile-menu-button btn btn-primary btn-block">
                        <span>...</span>
                        <span class="button-count">
                        (...)
                        <i class="glyphicon glyphicon-chevron-down"></i>
                    </span>
                    </a>
                {% endif %}
            </div>
        {% endif %}
    </div>
{% endblock %}

{% block content %}
    {% set numberOfFilterOnMobile = 0 %}
    {% if searchForm.type is defined %}{% set numberOfFilterOnMobile = numberOfFilterOnMobile + 1 %}{% endif %}
    {% if searchForm.availableSlot is defined %}{% set numberOfFilterOnMobile = numberOfFilterOnMobile + 1 %}{% endif %}
    <div
            class="catalog catalog--advanced
            {% if numberOfFilterOnMobile > 0 %}
                catalog-mobile-margin-{{ numberOfFilterOnMobile }}
            {% endif %}
        "
    >
        {{ include('EventBundle:Tip:tip.html.twig', {tipTranslationViews: tipTranslationViews}) }}
        {{ include('EventBundle:Catalog:Partial/catalog.html.twig', {
            searchForm: searchForm,
            typeViews: typeViews,
            categoryViews: categoryViews,
            paginatedResult: paginatedResult,
            sheet: sheet,
            seeMoreButton: seeMoreButton,
            page: page,
            isCatalog: isCatalog,
            tipTranslationViews: null,
        }, false) }}
    </div>
    {{ include('EventBundle:MeetingRequest:modalMeetingRequest.html.twig') }}
{% endblock %}
