<div class="meet lock" data-beginHour="{{ unavailability.begin|date('H:i') }}" data-duration="{{ unavailability.duration.format('%H:%I') }}">
    {{ include('EventBundle:Unavailability:header.html.twig', {unavailability: unavailability}) }}
</div>
