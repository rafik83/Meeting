<div class="meet lock" data-beginHour="{{ unavailability.begin|date('H:i', timezone) }}" data-duration="{{ unavailability.duration.format('%H:%I') }}">
    {{ include('EventBundle:Unavailability:header.html.twig', {
        timezone: timezone,
        unavailability: unavailability,
        'participantId': participantId,
        'sheetId': sheetId,
        'isUnavailabilityManagementDisabled': isUnavailabilityManagementDisabled,
    }) }}
</div>
