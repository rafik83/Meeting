{% extends "form_div_layout.html.twig" %}

{%- block form_row -%}
    <li>
        {{- form_label(form) -}}
        {{- form_widget(form, {attr: {class: errors|length ? 'error' : ''}}) -}}
    </li>
{%- endblock form_row -%}
