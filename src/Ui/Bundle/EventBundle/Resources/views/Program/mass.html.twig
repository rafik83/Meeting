{% set withHourPrefix = (withHourPrefix is defined and withHourPrefix == true) %}
{{ include('EventBundle:Unavailability/Mass:mass.html.twig', {'timezone': timeZone, 'mass': mass, 'withHourPrefix': withHourPrefix}) }}
