{% extends 'EventBundle:Register:base.html.twig' %}

{% form_theme form '@Ui/default.html.twig' %}

{% block title event.title %}

{% block content %}
    <div class="profile__content">
        <section class="main">
            <header class="registerStepHeader">
                <h1>
                    {{ stepTitle }}
                    {% if stepsCount > 1 %}
                        <span class="steps">- {{ 'register.step'|trans }} 1/{{ stepsCount }}</span>
                    {% endif %}
                </h1>
                <p>{{ stepDescription|html }}</p>
            </header>

            <div class="main__content">
                {{ form_start(form, {attr: {class: 'styled-form'}}) }}

                    {{ form_errors(form) }}

                    <div class="inputs">
                        {% for field in form %}
                            {{ form_row(field) }}
                        {% endfor %}

                        <div class="validation">
                            <button type="submit" class="btn btn-primary">
                                {% if stepsCount > 1 %}
                                    <i class="icon-Suivant_3"></i> {{ 'common.next'|trans }}
                                {% else %}
                                    <i class="icon-Valider_3"></i> {{ 'register.finalize'|trans }}
                                {% endif %}
                            </button>
                        </div>
                    </div>

                {{ form_end(form) }}
            </div>
        </section>
    </div>
{% endblock %}
