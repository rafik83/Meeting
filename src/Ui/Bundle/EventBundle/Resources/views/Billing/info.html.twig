{% extends 'EventBundle::base.html.twig' %}

{% block title 'billing_info.title'|trans %}

{% block content %}
    <div class="profile__content">
        <section class="main">
            <header>
                <h1>{{ 'billing_info.title'|trans }}</h1>
            </header>
            <div class="main__content">
                {{ form(form, { 'attr': { 'class': 'styled-form' } }) }}
            </div>
        </section>
    </div>
{% endblock %}
