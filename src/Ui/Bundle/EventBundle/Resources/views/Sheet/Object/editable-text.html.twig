
{% if config.type == 'title' %}

    <header>
        <h2>{{ data[uid]['text'][locale] }}</h2>
    </header>

{% elseif config.type == 'text' %}

    <p>{{ data[uid]['text'][locale]|nl2br }}</p>

{% endif %}
