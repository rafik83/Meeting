
<section id="object-{{ uid }}" class="object object--text {{ 'style--' ~ config['style'] }} section">
    <header>
        <h2>{{ config['content']|localize(locale, fallback) }}</h2>
    </header>
</section>
