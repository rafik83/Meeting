<section id="object-{{ uid }}" class="object object--collection {{ 'style--' ~ item.style }} section">
    <header>
        <h2>{{ item.label(locale, fallback) }}{% if item.required|default(false) %}*{% endif %}</h2>
    </header>
    <div class="section__content">
        <ul>
            {% for tag in item.tags %}
                <li>
                    {% if not isCatalog %}
                        <a href="{{ path('event_account_participant_company', {'sheet': sheet.id}) }}" >
                    {% endif %}

                    {{ tag['label']|localize(locale, fallback) }} :
                    {% if tag.tag in ["sheet_country", "participant_country"] %}
                        {% for taggedInfo in taggedData[tag.tag]|default([]) %}
                            {{ taggedInfo|country }} {% if loop.index < loop.length %}, {% endif %}
                        {% endfor %}
                    {% else %}
                        {{ taggedData[tag.tag]|default([])|join(', ') }}
                    {% endif %}

                    {% if not isCatalog %}
                        </a>
                    {% endif %}
                </li>
            {% endfor %}

            {% if item.collectionEnabled %}
                {% for list_item in item.data['items']|default([]) %}
                    <li>{{ list_item['title']|localize(locale, fallback) }}</li>
                {% endfor %}
            {% endif %}
        </ul>
    </div>
    {% if item.collectionEnabled and not isCatalog %}
        <footer>
            <a href="{{ path('event_sheet_form', { 'locale': locale, 'key': uid }) }}" class="edit-link" data-toggle="modal" data-target="#edit-modal"><i class="icon-Note_2"></i> {{ 'sheet.object.action.edit'|trans }} "{{ item.label(locale, fallback) }}"</a>
        </footer>
    {% endif %}
</section>
