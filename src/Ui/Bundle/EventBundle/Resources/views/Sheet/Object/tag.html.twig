
<div id="object-{{ uid }}" class="object object--tag {{ 'style--' ~ item.style }}">
    <header>
        <h2>{{ item.label(locale, fallback) }}</h2>
    </header>
    <div class="section__content">
        <p>{{ taggedData[item.tag]|join(', ')|default }}</p>
    </div>
</div>
