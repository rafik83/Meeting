<!DOCTYPE html>
<html lang="{{ locale }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="user-scalable=no, initial-scale=1, maximum-scale=1, minimum-scale=1, width=device-width" />
    <link rel="stylesheet" href="{{ absolute_url(asset('assets/css/mainStyle.css')) }}">
    <link rel="stylesheet" href="{{ absolute_url(asset(event.assetPath)) }}">
    <meta name="theme-color" content="#ffffff">
    <title>{{ event.title }}: {{ 'sheet.title'|trans }}</title>
</head>
<body>
    <div class="main-content container-fluid">
        {{ include('AdminBundle:Sheet/Pdf:sheet-content.html.twig') }}
    </div>
</body>
</html>
