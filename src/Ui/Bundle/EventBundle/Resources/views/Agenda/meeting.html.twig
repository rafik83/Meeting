{% set timezone = meeting.timeZone %}
{% if isVisio and participant.timezone %}
    {% set timezone = participant.timezone %}
{% endif %}

{{ include('EventBundle:Meeting:meeting.html.twig', {
    'timezone': timezone,
    'sheetId': sheetId,
    'participantId': participantId,
    'meeting': meeting,
    'canUpdateMeeting': canUpdateMeeting,
    'canRemoveMeeting' : canRemoveMeeting
}) }}
