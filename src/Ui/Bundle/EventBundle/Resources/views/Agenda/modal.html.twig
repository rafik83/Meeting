<div class="modal clear-on-hidden-modal fade {% if form_unavailability is defined or sendCodeForm is not null %}show-modal{% endif %}" id="unavailability-modal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            {% if form_unavailability is defined %}
                {{ include('EventBundle:Unavailability:create-form.html.twig') }}
            {% endif %}
            {% if sendCodeForm is not null %}
                {{ include('EventBundle:User/Phone/Modal:create-form.html.twig') }}
            {% endif %}
        </div>
    </div>
</div>
