{% set timezone = unavailability.timeZone %}
{% if isVisio and participant.timezone %}
    {% set timezone = participant.timezone %}
{% endif %}

{{ include('EventBundle:Unavailability:unavailability.html.twig', {
    'timezone': timezone,
    unavailability: unavailability,
    'participantId': participantId,
    'sheetId': sheetId,
    'isUnavailabilityManagementDisabled': isUnavailabilityManagementDisabled,
}) }}
