{% set timezone = unavailability.timeZone %}
{% if participant.visio and participant.timezone %}
    {% set timezone = participant.timezone %}
{% endif %}

{{ include('EventBundle:Unavailability:unavailability.html.twig', { 'timezone': timezone, unavailability: unavailability, 'participantId': participantId, 'sheetId': sheetId}) }}
