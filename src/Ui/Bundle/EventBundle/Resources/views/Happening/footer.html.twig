<footer>
    <p class="hours">
        <i class="icon icon-Horaire"></i>
        <span>{{ beginHour|date('H:i', happening.timeZone) }} - {{ endHour|date('H:i', happening.timeZone) }}</span>
    </p>
    {% if isActionButtonsOnDetails %}
        {{ include('EventBundle:Happening:actions.html.twig', {
            'sheet': sheet,
            'happening': happening,
            'isUserAloneParticipant': isUserAloneParticipant,
            'doesCurrentUserParticipate': doesCurrentUserParticipate,
        }) }}
    {% endif %}
</footer>
