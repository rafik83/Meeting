<div class="modal fade clear-on-hidden-modal {% if displayAddParticipantForm or displayRemoveParticipantForm %}show-modal{% endif %}" id="edit-modal" tabindex="-1" role="dialog" data-placeholder="{{ '<div class="modal-body text-center"><i class="glyphicon glyphicon-refresh spin text-xl"></i></div>'|e }}">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            {% if form_add is defined and displayAddParticipantForm %}
                {# NB: need to be replaced by a select product from several products #}
                {% set participantProductView = view.products.participantsView.participantProductViews|first %}

                {{ include('EventBundle:Participant:add.html.twig', { 'form': form_add, 'sheet': sheet, 'backRoute': 'backToPackage', 'participantProductView': participantProductView }) }}
            {% elseif form_remove is defined and displayRemoveParticipantForm %}
                {% set participantProductView = view.products.participantsView.participantProductViews|first %}

                {{ include('EventBundle:Participant:removeFromPackage.html.twig', { 'form': form_remove, 'participants': participants, 'label': participantProductView.title }) }}
            {% endif %}
        </div>
    </div>
</div>
