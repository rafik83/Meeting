{% if groups.groups is defined and groups.groups is not empty %}
    {% for group in groups.groups %}
        {{ include('EventBundle:Order/Summary:group.html.twig', {group: group, order:order}) }}
    {% endfor %}
{% endif %}
