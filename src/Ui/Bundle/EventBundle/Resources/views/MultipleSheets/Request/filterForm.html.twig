<div class="row">
    {{ form_start(filterForm) }}
    <table class="table">
        <thead>
            <tr>
                <th class="col-md-3">
                    {{ form_label(filterForm.otherSheet) }}
                </th>
                <th class="col-md-2">
                    {{ form_label(filterForm.state) }}
                </th>
                <th class="col-md-2">
                    {{ form_label(filterForm.type) }}

                </th>
                <th class="col-md-3"></th>
                <th class="col-md-2"></th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <th class="col-md-3">
                    {{ form_widget(filterForm.otherSheet) }}
                </th>
                <th class="col-md-2">
                    {{ form_widget(filterForm.state) }}
                </th>
                <th class="col-md-2">
                    {{ form_widget(filterForm.type) }}
                </th>
                <th class="col-md-3"></th>
                <th class="col-md-2"></th>
            </tr>
        </tbody>
    </table>
    <div class="text-right">{{ form_row(filterForm.submit) }}</div>
    {{ form_end(filterForm) }}
</div>
