{% extends 'EventBundle:Register:base.html.twig' %}

{% block title 'logout.title'|trans %}

{% block content %}
    <section class="main">
        <header>
            <h1><i class="icon-Clef"></i> {{ 'logout.title'|trans }}</h1>
        </header>

        <div class="main__content">
            <ul class="inputs">
                <li>
                    {{ 'logout.confirmation'|trans }}
                </li>

                <li class="validation">
                    <a href="{{ path('event_logout') }}" class="btn btn-primary">{{ 'logout.link'|trans }}</a>
                </li>
            </ul>
        </div>
    </section>
{% endblock %}
