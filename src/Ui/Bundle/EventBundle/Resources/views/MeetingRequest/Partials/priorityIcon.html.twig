{% if isPriority is defined and isPriority %}
    <span title="{{ 'event.meeting.request.priority'|trans() }}">
         <i class="rating__icon rating__icon--star icon-EtoilePleine"></i>
    </span>
{% endif %}
