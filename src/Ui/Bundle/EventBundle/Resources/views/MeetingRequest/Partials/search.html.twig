{{ form_start(searchForm) }}

<div class="catalog__research">
    <div class="filter">
        <h3>{{ searchForm.vars.label|trans({}, searchForm.vars.translation_domain) }}</h3>

        {#<ul data-message-at-least-one-checked="{{ ('form.search.' ~ searchForm.vars.name  ~ '.at_least_one_checked')|trans({}, 'forms') }}">#}
            {#{% for child in searchForm.children %}#}
                {#{% set disabled = choices[child.vars.value].count == 0 %}#}
                {#<li class="filter__item">#}
                    {#<label class="checkbox-inline {{ disabled ? 'count_zero' : '' }}">#}
                        {#<span class="filter-label">{{ form_widget(child, {disabled: disabled}) }}{{ child.vars.label|trans({}, child.vars.translation_domain) }}</span>#}
                        {#<span class="details">{{ choices[child.vars.value].count }}</span>#}
                    {#</label>#}
                {#</li>#}
            {#{% endfor %}#}
        {#</ul>#}
    </div>

</div>

<div class="catalog__filters">

    <div class="select-all">
        {#
        <label class="checkbox-inline" for="select-all-catalog">
            <input type="checkbox" id="select-all-catalog">
            Tout sélectionner (40)
        </label>
        #}
    </div>

    <div class="sort">
        {#{{ include('EventBundle:Catalog:order-by-form.html.twig', { form: searchForm.orderBy }, false) }}#}

        {#
        <div class="btn-group" role="group">
            <button type="button" class="btn btn-default active"><i class="icon-Mosaique"></i></button>
            <button type="button" class="btn btn-default"><i class="icon-Liste"></i></button>
        </div>
        #}
    </div>
</div>

{{ form_end(searchForm) }}
