{% extends 'EventBundle::base.html.twig' %}

{% form_theme form '@Ui/default.html.twig' %}

{% block title event.title %}

{% block content %}
    <div class="profile__content">
        <section class="main">
            <header>
                <h1><i class="icon-Societe"></i> {{ 'account.update.company.title'|trans }}</h1>
            </header>

            <div class="main__content">
                {{ form_start(form, {attr: {class: 'styled-form'}}) }}

                {{ form_errors(form) }}

                <div class="inputs" data-company-info-update="true">
                    {% for child in form %}
                        {% if child.vars.data.tagsWithoutSetters is defined %}
                            {% for tag in child.vars.data.tagsWithoutSetters %}
                                <a name="field_{{ tag }}"></a>
                            {% endfor %}
                        {% endif %}
                        {{ form_row(child) }}
                        {% include '@Event/uploadedObjectPreview.html.twig' %}
                    {% endfor %}

                    <div class="validation">
                        <button type="submit" class="btn btn-primary">
                            <i class="icon-Suivant_3"></i> {{ 'common.validate'|trans }}
                        </button>
                    </div>
                </div>

                {{ form_end(form) }}
            </div>
        </section>
    </div>
{% endblock %}
