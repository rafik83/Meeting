{% block subject %}
{{ 'mail.resetPassword.subject'|trans({'%event%': mail.eventTitle}, 'mail', mail.locale) }}
{% endblock %}

{% block body %}
{{ 'mail.resetPassword.body'|trans({}, 'mail', mail.locale)|html|nl2br }}
<a href="{{ absolute_url(path('event_create_new_password', {
    'token': mail.token
})) }}">{{ absolute_url(path('event_create_new_password', {
        'token': mail.token
    })) }}</a><br>
{{ 'mail.resetPassword.end'|trans({}, 'mail', mail.locale)|html|nl2br }}
{% endblock %}
