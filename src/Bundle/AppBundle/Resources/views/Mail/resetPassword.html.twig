{% block subject %}
{{ 'mail.resetPassword.subject'|trans({'%event%': mail.eventTitle}, 'mail') }}
{% endblock %}

{% block body %}
{{ 'mail.resetPassword.body'|trans({}, 'mail')|html|nl2br }}
<a href="{{ absolute_url(path('event_create_new_password', {
    'token': mail.token
})) }}">{{ absolute_url(path('event_create_new_password', {
        'token': mail.token
    })) }}</a><br>
{{ 'mail.resetPassword.end'|trans({}, 'mail')|html|nl2br }}
{% endblock %}
