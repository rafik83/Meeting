<div>
    {% for locale in locales %}
        <a href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({ '_locale' : locale })) }}">{{ locale|locale(locale) }}</a> |
    {% endfor %}
</div>
