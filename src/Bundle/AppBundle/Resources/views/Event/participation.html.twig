{% extends 'VimeetAppBundle::base.html.twig' %}

{% block title event.title %}

{% block body %}
    <h1>{{ event.title }}</h1>
    <div class="row">
        <div class="col-md-4 col-md-offset-4">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
