{% extends 'VimeetAppBundle::base.html.twig' %}

{% block body %}
    <h1>
        <a href="{{ path('event') }}">{{ eventView.title }}</a>
    </h1>

    {{ include('VimeetAppBundle::flashes.html.twig') }}

    {% block content %}{% endblock %}

{% endblock %}
