{% extends 'VimeetAppBundle::base.html.twig' %}

{% block body %}
    <h1>
        <a href="{{ path('event', { 'subdomain': app.request.attributes.get('subdomain') }) }}">{{ eventView.title }}</a>
    </h1>

    {{ include('VimeetAppBundle::flashes.html.twig') }}

    {% block content %}{% endblock %}

{% endblock %}
