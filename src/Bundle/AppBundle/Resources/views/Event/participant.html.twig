<table class="table">
    {% for key, field in template %}
        <tr>
            <th>{{ field['label'][app.request.locale]|default }}</th>
            <td>{{ data[key]|default }}</td>
        </tr>
    {% endfor %}
</table>
