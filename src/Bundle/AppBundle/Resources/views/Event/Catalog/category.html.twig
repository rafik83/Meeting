{% extends 'VimeetAppBundle::base.html.twig' %}

{% block body %}
    {{ include('VimeetAppBundle:Event:title.html.twig') }}
    <h2>Catalogue "{{ category.title }}"</h2>

    {% for sheet in sheets %}
        {{ include(template_from_string(sheet.type.previewTemplate), { 'sheet': sheet }) }}
    {% endfor %}
{% endblock %}
