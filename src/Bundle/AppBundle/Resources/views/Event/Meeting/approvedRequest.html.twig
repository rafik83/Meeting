{% extends 'VimeetAppBundle:Event:base.html.twig' %}

{% block title 'event.meeting.approvedRequest.title'|trans %}

{% block content %}
    <h1>{{ 'event.meeting.approvedRequest.title'|trans }}</h1>
    <h2>{{ 'event.meeting.approvedRequest.label'|trans({'%toName%': toName, '%fromName%': fromName}) }}</h2>
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
