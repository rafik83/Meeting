{% extends 'VimeetAppBundle:Event:base.html.twig' %}

{% block title eventView.title %}

{% block content %}
    <table class="table">
        <caption>
            {{ meeting.meetingSlot.schedule.date|date('d/m/Y') }}
            de {{ meeting.meetingSlot.begin|date('H:i') }}
            à {{ meeting.meetingSlot.end|date('H:i') }}
        </caption>
        <tr>
            <th>Fiches</th>
            <td>{{ from }}</td>
            <td>{{ to }}</td>
        </tr>
        <tr>
            <th>Participants</th>
            <td>
                <ul>
                    {% for participant in fromParticipants %}
                        <li>{{ participant }}</li>
                    {% endfor %}
                </ul>
            </td>
            <td>
                <ul>
                    {% for participant in toParticipants %}
                        <li>{{ participant }}</li>
                    {% endfor %}
                </ul>
            </td>
        </tr>
    </table>
    <a href="{{ path('event_sheet_schedule_meeting_cancel', { 'subdomain': app.request.attributes.get('subdomain'), 'id': sheet.id, 'schedule_id': schedule.id, 'meeting_id': meeting.id }) }}" class="btn btn-danger">Annuler</a>
{% endblock %}
