{% extends 'VimeetAppBundle:Event:base.html.twig' %}

{% form_theme form 'VimeetAppBundle::library.html.twig' %}

{% block title eventView.title %}

{% block content %}
    <h2>Mode de paiement</h2>
    <p>Choisissez votre mode de paiement</p>
    <a href="{{ path('event_sheet_billing_pro_forma', {
        'id'       : sheet.id,
        'subdomain': app.request.attributes.get('subdomain')
     }) }}" target="_blank">{{ 'event.sheet.billing.proForma'|trans }}</a>
    <hr>

    {{ form(form) }}

{% endblock %}
