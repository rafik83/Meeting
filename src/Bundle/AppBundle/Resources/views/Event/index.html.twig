{% extends 'VimeetAppBundle::base.html.twig' %}

{% block title event.title %}

{% block body %}
    {{ 'greetings'|trans }}
    {{ 'now'|format_datetime }}

    <h1>{{ event.title }}</h1>
    <p>{{ event.description }}</p>
{% endblock %}
