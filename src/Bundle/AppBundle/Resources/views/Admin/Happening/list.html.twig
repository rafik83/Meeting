{% extends 'VimeetAppBundle:Admin:base.html.twig' %}

{% block content %}
    <h1>{{ event.title }}</h1>
    <h2>Sous-événements</h2>
    <div class="text-right"><a href="{{ path('admin_happening_category_list', {id: event.id}) }}" class="btn btn-primary">Catégories de sous-événement</a></div>
    <div class="text-left">
        <a href="{{ path('admin_event_list') }}" class="btn btn-link">&larr; Retour aux événements</a>
    </div>
{% endblock %}
