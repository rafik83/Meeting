{% extends 'VimeetAppBundle:Admin:base.html.twig' %}

{% block content %}
    <h1>{{ event.title }}</h1>
    <h2>Catégories de sous-événement</h2>

    <table class="table">
        <thead>
        <tr>
            <th>Titre</th>
            <th>Picto</th>
            <th><a href="{{ path('admin_happening_category_create', { 'id': event.id }) }}" class="btn btn-success btn-sm">Ajouter</a></th>
        </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.title }}</td>
                <td>{{ category.picto }}</td>
                <td>
                    <a href="{{ path('admin_happening_category_update', { 'id': event.id, 'category': category.id }) }}" class="btn btn-default">Modifier</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="text-left">
        <a href="{{ path('admin_happening_list', {id: event.id}) }}" class="btn btn-link">&larr; Retour aux sous-événements</a>
    </div>
{% endblock %}
