{% extends 'VimeetAppBundle:Admin:base.html.twig' %}

{% block title 'Ajouter un champ' %}

{% block content %}
    <h1><a href="{{ path('admin_type_list', { 'event': event.id }) }}">{{ event.title }}</a></h1>
    <h2><a href="{{ path('admin_type_template_field_list', { 'event': event.id, 'type': type.id }) }}">{{ typeView.title }}</a></h2>
    <h3>{{ 'template.add'|trans }} {{ ('template.name.' ~ create.templateName)|trans }}</h3>
    <hr>

    {{ form(form) }}
{% endblock %}
