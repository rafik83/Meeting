{% extends 'VimeetAppBundle:Admin:base.html.twig' %}

{% form_theme form 'VimeetAppBundle:Admin/TypeTemplateField:theme.html.twig' %}

{% block title 'Modifier un champ' %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('assets/js/main.js') }}"></script>
{% endblock %}

{% block content %}
    <h1><a href="{{ path('admin_type_list', { 'event': event.id }) }}">{{ event.title }}</a></h1>
    <h2><a href="{{ path('admin_type_template_field_list', { 'event': event.id, 'type': type.id }) }}">{{ typeView.title }}</a></h2>
    <h3>{{ ('template.name.' ~ update.templateName)|trans }}</h3>
    <hr>

    {{ form(form) }}
{% endblock %}
