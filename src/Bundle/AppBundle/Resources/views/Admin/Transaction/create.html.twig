{% extends 'VimeetAppBundle:Admin:base.html.twig' %}

{% import 'VimeetAppBundle::macro.html.twig' as macro %}

{% block content %}
    <h1>{{ sheet_info }}</h1>

    <h2>Ajouter une transaction</h2>

    {{ form(form) }}

    <div class="text-left">
        <a href="{{ path('admin_sheet_billing', { 'id': sheet.event.id, 'sheet_id': sheet.id }) }}" class="btn btn-link">&larr; Retour aux commandes</a>
    </div>

{% endblock %}

