{% extends 'VimeetAppBundle:Admin:base.html.twig' %}

{% block content %}
    <h1>{{ sheet_info }}</h1>

    <h2>Commande {{ order.id }}</h2>

    <div class="text-left">
        <a href="{{ path('admin_sheet_order_list', { 'id': order.sheet.event.id, 'sheet_id': order.sheet.id }) }}" class="btn btn-link">&larr; Retour aux commandes</a>
    </div>

{% endblock %}

