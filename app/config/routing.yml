default:
    pattern: /
    defaults:
        _controller: FrameworkBundle:Redirect:urlRedirect
        path: /%locale%/
        permanent: true
    methods: [GET]

vimeet_app:
    resource: "@VimeetAppBundle/Resources/config/routing.yml"
    prefix:   /{_locale}
    defaults:
        _locale: %locale%
